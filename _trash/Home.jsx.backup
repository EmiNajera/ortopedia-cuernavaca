import React, { useRef, useState } from 'react';
import { Link } from 'react-router-dom';
import { motion, useInView, useScroll, useTransform } from 'framer-motion';
import Layout from "../../components/layout/Layout";


// --- Iconos SVG para una apariencia limpia y moderna ---

// Icono para características clave y botones
const ArrowRightIcon = () => (
  <svg xmlns="http://www.w3.org/2000/svg" className="h-5 w-5 ml-2 inline" viewBox="0 0 20 20" fill="currentColor">
    <path fillRule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clipRule="evenodd" />
  </svg>
);

// Iconos para la sección de características clave
const InsolesIcon = () => (
    <svg className="w-12 h-12 text-cyan-500" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M17.657 18.657A8 8 0 016.343 7.343S7 9 9 10s5 2 7 0l-1.657 1.657a4 4 0 01-5.656 0l-1.415-1.414m-2.828 2.828a4 4 0 010-5.656l1.415-1.414m5.656 0a8.003 8.003 0 010 11.314M9 10l.343-.343"></path></svg>
);
const ProsthesisIcon = () => (
    <svg className="w-12 h-12 text-cyan-500" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path><path d="M18 18.5a4.5 4.5 0 10-9 0 4.5 4.5 0 009 0zM12 12v6.5"></path></svg>
);
const RehabIcon = () => (
    <svg className="w-12 h-12 text-cyan-500" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path></svg>
);


// --- Componentes Reutilizables ---

// Tarjeta para la sección de Áreas Fundamentales con animaciones
const ServiceCard = ({ title, description, link, delay = 0 }) => (
  <motion.div 
    initial={{ opacity: 0, y: 50 }}
    whileInView={{ opacity: 1, y: 0 }}
    transition={{ duration: 0.6, delay }}
    whileHover={{ 
      scale: 1.05, 
      boxShadow: "0 20px 40px rgba(0,0,0,0.1)",
      transition: { duration: 0.3 }
    }}
    className="bg-white p-8 rounded-lg shadow-md hover:shadow-xl transition-all duration-300 flex flex-col cursor-pointer"
  >
    <motion.h3 
      className="text-2xl font-bold text-gray-900 mb-4"
      whileHover={{ color: "#2563eb" }}
    >
      {title}
    </motion.h3>
    <p className="text-gray-600 flex-grow">{description}</p>
    <Link to={link} className="text-blue-600 font-semibold mt-6 self-start hover:text-blue-800 transition-colors group">
      Ver más 
      <motion.span 
        className="inline-block ml-2"
        whileHover={{ x: 5 }}
        transition={{ duration: 0.2 }}
      >
        <ArrowRightIcon />
      </motion.span>
    </Link>
  </motion.div>
);

// Tarjeta para la sección de Tienda con animaciones mejoradas
const ProductCard = ({ imgSrc, name, delay = 0 }) => ( 
    <motion.div 
        initial={{ opacity: 0, scale: 0.9 }}
        whileInView={{ opacity: 1, scale: 1 }}
        transition={{ duration: 0.5, delay }}
        whileHover={{ 
            scale: 1.03,
            transition: { duration: 0.2 }
        }}
        className="bg-white rounded-lg shadow-sm overflow-hidden group cursor-pointer"
    >
        <div className="overflow-hidden">
            <motion.img 
                src={imgSrc} 
                alt={name} 
                className="w-full h-48 object-cover transition-transform duration-500" 
                whileHover={{ scale: 1.1 }}
            />
        </div>
        <motion.div 
            className="p-4"
            whileHover={{ backgroundColor: "#f8fafc" }}
        >
            <h4 className="text-lg font-bold text-gray-800">{name}</h4>
        </motion.div>
    </motion.div>
);

// --- Componente Principal de la Aplicación ---

// Datos para la sección de tienda interactiva
const categorias = [
  {
    nombre: "Calzado Ortopédico",
    cita: "Diseñado para brindar confort y apoyo postural en el día a día.",
    autor: "Colección 2025",
    empresa: "Ortopedia Cuernavaca",
    imagen: "https://placehold.co/1200x600/007BFF/FFFFFF?text=Calzado+Ortopédico",
    marcas: ["Zapatos Hombre", "Zapatos Mujer", "Sandalias Ortopédicas"],
    link: "/tienda?categoria=calzado"
  },
  {
    nombre: "Plantillas Ortopédicas",
    cita: "Soporte personalizado para mejorar tu pisada y postura.",
    autor: "Sistema Digital de Escaneo",
    empresa: "Ortopedia Cuernavaca",
    imagen: "https://placehold.co/1200x600/00BCD4/FFFFFF?text=Plantillas+Ortopédicas",
    marcas: ["Plantillas Pediátricas", "Plantillas Adulto", "Deportivas"],
    link: "/tienda?categoria=plantillas"
  },
  {
    nombre: "Órtesis y Soportes",
    cita: "Estabilidad y protección para tu recuperación y bienestar.",
    autor: "Líneas Profesionales",
    empresa: "Ortopedia Cuernavaca",
    imagen: "https://placehold.co/1200x600/1E40AF/FFFFFF?text=Órtesis+y+Soportes",
    marcas: ["Rodilleras", "Fajas Lumbares", "Muñequeras"],
    link: "/tienda?categoria=ortesis"
  }
];

const Home = () => {
  const heroRef = useRef(null);
  const [activo, setActivo] = useState(categorias[0]);
  const { scrollYProgress } = useScroll({
    target: heroRef,
    offset: ["start start", "0.2 0.8"]
  });
  const y = useTransform(scrollYProgress, [0, 1], ["0%", "30%"]);
  const opacity = useTransform(scrollYProgress, [0, 1], [1, 0.3]);

  return (
    <Layout>
      {/* Contenedor principal con stacking effect */}
      <div className="relative">
                 {/* Sección 1 – Hero Principal con efecto parallax */}
         <motion.section 
           ref={heroRef}
           className="relative h-screen flex items-center justify-center text-center bg-cover bg-center overflow-hidden" 
           style={{ backgroundImage: "url('/images/banners/Protesis TiE.jpg')" }}
        >
          
          <motion.div 
            className="relative z-10 p-6"
            initial={{ opacity: 0, y: 100 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 1, ease: "easeOut" }}
          >
                                       <motion.h1 
                className="text-6xl md:text-8xl font-bold text-white drop-shadow-2xl" 
                style={{ fontFamily: "'Poppins', sans-serif", textShadow: "2px 2px 8px rgba(0,0,0,0.8)" }}
                initial={{ opacity: 0, y: 50 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.8, delay: 0.2 }}
              >
                Rehabilitación física y ortopedia
              </motion.h1>
              <motion.p 
                className="text-2xl md:text-3xl mt-6 max-w-4xl mx-auto text-white drop-shadow-2xl"
                style={{ textShadow: "1px 1px 6px rgba(0,0,0,0.7)" }}
                initial={{ opacity: 0, y: 30 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.8, delay: 0.4 }}
              >
                Te acompañamos con tratamientos personalizados desde el diagnóstico hasta la recuperación.
              </motion.p>
            <motion.div 
              className="mt-10 flex flex-col sm:flex-row justify-center gap-4"
              initial={{ opacity: 0, y: 30 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.8, delay: 0.6 }}
            >
              <motion.div
                whileHover={{ scale: 1.05 }}
                whileTap={{ scale: 0.95 }}
              >
                <Link to="/citas" className="bg-blue-600 text-white font-bold py-4 px-8 rounded-lg hover:bg-blue-700 transition-colors text-lg inline-block shadow-2xl" style={{ boxShadow: "0 10px 25px rgba(0,0,0,0.3)" }}>
                  Agenda tu cita
                </Link>
              </motion.div>
              <motion.div
                whileHover={{ scale: 1.05 }}
                whileTap={{ scale: 0.95 }}
              >
                <Link to="/servicios" className="bg-white/90 backdrop-blur-sm border-2 border-white text-blue-600 font-bold py-4 px-8 rounded-lg hover:bg-white hover:text-blue-700 transition-all duration-300 text-lg inline-block shadow-2xl" style={{ boxShadow: "0 10px 25px rgba(0,0,0,0.2)" }}>
                  Explora nuestros servicios
                </Link>
              </motion.div>
            </motion.div>
          </motion.div>
        </motion.section>

                          {/* Sección 2 – Áreas Fundamentales con superposición */}
          <motion.section 
            id="servicios" 
            className="relative z-20 -mt-40"
            initial={{ opacity: 0, y: 100, scale: 0.95 }}
            whileInView={{ opacity: 1, y: 0, scale: 1 }}
            transition={{ duration: 1.2, ease: "easeOut" }}
            viewport={{ once: true, margin: "-100px" }}
         >
          <motion.div 
            className="grid grid-cols-1 md:grid-cols-3 h-screen pt-20"
            style={{ y }}
            initial={{ opacity: 0 }}
            whileInView={{ opacity: 1 }}
            transition={{ duration: 0.8, delay: 0.3 }}
            viewport={{ once: true }}
          >
    
    {/* Órtesis y Prótesis */}
    <motion.div 
      className="relative group overflow-hidden cursor-pointer"
      initial={{ opacity: 0, x: -100 }}
      whileInView={{ opacity: 1, x: 0 }}
      transition={{ duration: 0.8, delay: 0.1 }}
      whileHover={{ scale: 1.02 }}
      viewport={{ once: true }}
    >
             <motion.img
         src="/images/banners/Orthesis KHAFO 2 FB.PNG"
         alt="Órtesis y Prótesis"
         className="w-full h-full object-cover"
         whileHover={{ scale: 1.1 }}
         transition={{ duration: 0.6 }}
       />
      <motion.div 
        className="absolute inset-0 bg-black bg-opacity-40 flex flex-col items-center justify-center text-center text-white p-8"
        whileHover={{ backgroundColor: "rgba(0, 0, 0, 0.6)" }}
        transition={{ duration: 0.3 }}
      >
        <motion.h3 
          className="text-4xl font-bold mb-4"
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.6, delay: 0.3 }}
        >
          Órtesis y Prótesis
        </motion.h3>
        <motion.p 
          className="text-lg max-w-md"
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.6, delay: 0.5 }}
        >
          Adaptamos prótesis y órtesis con enfoque personalizado para cada paciente.
        </motion.p>
      </motion.div>
    </motion.div>

    {/* Plantillas Personalizadas */}
    <motion.div 
      className="relative group overflow-hidden cursor-pointer"
      initial={{ opacity: 0, y: 100 }}
      whileInView={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.8, delay: 0.2 }}
      whileHover={{ scale: 1.02 }}
      viewport={{ once: true }}
    >
             <motion.img
         src="/images/banners/Plantillas Ortopedicas A.PNG"
         alt="Plantillas Personalizadas"
         className="w-full h-full object-cover"
         whileHover={{ scale: 1.1 }}
         transition={{ duration: 0.6 }}
       />
      <motion.div 
        className="absolute inset-0 bg-black bg-opacity-40 flex flex-col items-center justify-center text-center text-white p-8"
        whileHover={{ backgroundColor: "rgba(0, 0, 0, 0.6)" }}
        transition={{ duration: 0.3 }}
      >
        <motion.h3 
          className="text-4xl font-bold mb-4"
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.6, delay: 0.4 }}
        >
          Plantillas Personalizadas
        </motion.h3>
        <motion.p 
          className="text-lg max-w-md"
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.6, delay: 0.6 }}
        >
          Plantillas con análisis de marcha y fabricación digital para mejorar postura y movilidad.
        </motion.p>
      </motion.div>
    </motion.div>

    {/* Fisioterapia y Rehabilitación */}
    <motion.div 
      className="relative group overflow-hidden cursor-pointer"
      initial={{ opacity: 0, x: 100 }}
      whileInView={{ opacity: 1, x: 0 }}
      transition={{ duration: 0.8, delay: 0.3 }}
      whileHover={{ scale: 1.02 }}
      viewport={{ once: true }}
    >
             <motion.img
         src="/images/banners/Fisioterapia 2.png"
         alt="Fisioterapia y Rehabilitación"
         className="w-full h-full object-cover"
         whileHover={{ scale: 1.1 }}
         transition={{ duration: 0.6 }}
       />
      <motion.div 
        className="absolute inset-0 bg-black bg-opacity-40 flex flex-col items-center justify-center text-center text-white p-8"
        whileHover={{ backgroundColor: "rgba(0, 0, 0, 0.6)" }}
        transition={{ duration: 0.3 }}
      >
        <motion.h3 
          className="text-4xl font-bold mb-4"
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.6, delay: 0.5 }}
        >
          Fisioterapia y Rehabilitación
        </motion.h3>
        <motion.p 
          className="text-lg max-w-md"
          initial={{ opacity: 0, y: 20 }}
          whileInView={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.6, delay: 0.7 }}
        >
          Recupera tu movimiento con terapias de rehabilitación física profesional.
        </motion.p>
      </motion.div>
    </motion.div>
  </motion.div>
        </motion.section>
      </div>

      {/* Sección 3 – Tienda Ortopédica */}
      <section id="tienda" className="py-20 bg-white">
        <div className="container mx-auto px-6">
          <h2 className="text-4xl font-bold text-center mb-10">
            Todo lo que necesitas para tu bienestar
          </h2>

          {/* Pestañas de categorías */}
          <div className="flex flex-wrap justify-center gap-4 mb-10">
            {categorias.map((item) => (
              <button
                key={item.nombre}
                onClick={() => setActivo(item)}
                className={`px-4 py-2 rounded-full font-medium transition ${
                  activo.nombre === item.nombre
                    ? "bg-blue-600 text-white"
                    : "bg-gray-100 text-gray-700 hover:bg-gray-200"
                }`}
              >
                {item.nombre}
              </button>
            ))}
          </div>

          {/* Contenido dinámico */}
          <div className="relative rounded-xl overflow-hidden shadow-lg">
            <img
              src={activo.imagen}
              alt={activo.nombre}
              className="w-full h-96 object-cover transition-transform duration-500"
            />
            <div className="absolute bottom-0 left-0 p-6 bg-white/90 rounded-tr-xl w-full md:w-1/2">
              <p className="italic text-gray-700 mb-4">"{activo.cita}"</p>
              <p className="font-semibold text-gray-900">{activo.autor}</p>
              <p className="text-gray-500 text-sm">{activo.empresa}</p>
            </div>

            <div className="absolute top-0 right-0 p-6 space-y-3">
              {activo.marcas.map((marca, index) => (
                <Link
                  key={index}
                  to={activo.link}
                  className="block bg-white rounded-lg shadow px-4 py-2 text-sm font-medium hover:bg-gray-100 transition"
                >
                  {marca}
                </Link>
              ))}
            </div>
          </div>
        </div>
      </section>

        {/* Sección 4 – Historias de Pacientes con animaciones */}
        <motion.section 
          className="py-20 bg-gray-100"
          initial={{ opacity: 0 }}
          whileInView={{ opacity: 1 }}
          transition={{ duration: 0.8 }}
          viewport={{ once: true }}
        >
          <div className="container mx-auto px-6 text-center">
            <motion.h2 
              className="text-4xl font-bold text-black mb-12"
              initial={{ opacity: 0, y: 50 }}
              whileInView={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.8 }}
              viewport={{ once: true }}
            >
              Nuestros pacientes, nuestra inspiración
            </motion.h2>
            <div className="grid md:grid-cols-3 gap-10">
              {[
                {
                  image: "https://placehold.co/100x100/cccccc/FFFFFF?text=Paciente+1",
                  text: "Gracias a sus plantillas personalizadas, mi dolor de pie desapareció en semanas.",
                  delay: 0.2
                },
                {
                  image: "https://placehold.co/100x100/cccccc/FFFFFF?text=Paciente+2",
                  text: "La prótesis me devolvió movilidad y seguridad para mi vida diaria.",
                  delay: 0.4
                },
                {
                  image: "https://placehold.co/100x100/cccccc/FFFFFF?text=Paciente+3",
                  text: "La fisioterapia aceleró mi recuperación después de una cirugía.",
                  delay: 0.6
                }
              ].map((testimonial, index) => (
                <motion.div 
                  key={index}
                  className="bg-white p-6 rounded-lg shadow-sm hover:shadow-lg transition-shadow duration-300"
                  initial={{ opacity: 0, y: 50 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  transition={{ duration: 0.8, delay: testimonial.delay }}
                  whileHover={{ 
                    scale: 1.05,
                    boxShadow: "0 10px 30px rgba(0,0,0,0.1)"
                  }}
                  viewport={{ once: true }}
                >
                  <motion.img 
                    src={testimonial.image} 
                    alt={`Paciente ${index + 1}`} 
                    className="w-24 h-24 rounded-full mx-auto mb-4 border-4 border-cyan-400"
                    whileHover={{ scale: 1.1, rotate: 5 }}
                    transition={{ duration: 0.3 }}
                  />
                  <motion.p 
                    className="italic text-gray-600"
                    initial={{ opacity: 0 }}
                    whileInView={{ opacity: 1 }}
                    transition={{ duration: 0.6, delay: testimonial.delay + 0.2 }}
                  >
                    "{testimonial.text}"
                  </motion.p>
                </motion.div>
              ))}
            </div>
          </div>
        </motion.section>

        {/* Sección 5 – Tecnología con animaciones */}
        <motion.section 
          className="py-24 bg-blue-700 text-white"
          initial={{ opacity: 0 }}
          whileInView={{ opacity: 1 }}
          transition={{ duration: 0.8 }}
          viewport={{ once: true }}
        >
          <div className="container mx-auto px-6 text-center">
            <motion.h2 
              className="text-4xl font-bold mb-4"
              initial={{ opacity: 0, y: 50 }}
              whileInView={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.8 }}
              viewport={{ once: true }}
            >
              Precisión en cada paso
            </motion.h2>
            <motion.p 
              className="text-xl max-w-3xl mx-auto mb-8 text-blue-100"
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.8, delay: 0.2 }}
              viewport={{ once: true }}
            >
              Integramos análisis biomecánico, fabricación digital y tecnología de punta para personalizar cada solución.
            </motion.p>
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.8, delay: 0.4 }}
              viewport={{ once: true }}
            >
              <motion.div
                whileHover={{ scale: 1.05 }}
                whileTap={{ scale: 0.95 }}
              >
                <Link to="/servicios" className="bg-white text-blue-700 font-bold py-3 px-8 rounded-lg hover:bg-gray-200 transition-colors inline-block">
                  Ver cómo funciona
                </Link>
              </motion.div>
            </motion.div>
          </div>
        </motion.section>
        
        {/* Sección 6 – Features Clave con animaciones */}
        <motion.section 
          className="py-20 bg-white"
          initial={{ opacity: 0 }}
          whileInView={{ opacity: 1 }}
          transition={{ duration: 0.8 }}
          viewport={{ once: true }}
        >
            <div className="container mx-auto px-6 grid md:grid-cols-3 gap-12 text-center">
                {[
                  {
                    icon: <InsolesIcon />,
                    title: "Plantillas Ortopédicas",
                    description: "Confort y corrección para tu pisada diaria.",
                    delay: 0.1
                  },
                  {
                    icon: <ProsthesisIcon />,
                    title: "Prótesis Personalizadas",
                    description: "Adaptación perfecta a cada paciente para máxima funcionalidad.",
                    delay: 0.3
                  },
                  {
                    icon: <RehabIcon />,
                    title: "Rehabilitación Tecnológica",
                    description: "Uso de sensores y técnicas modernas para una recuperación eficaz.",
                    delay: 0.5
                  }
                ].map((feature, index) => (
                  <motion.div 
                    key={index}
                    className="flex flex-col items-center"
                    initial={{ opacity: 0, y: 50 }}
                    whileInView={{ opacity: 1, y: 0 }}
                    transition={{ duration: 0.8, delay: feature.delay }}
                    whileHover={{ y: -10 }}
                    viewport={{ once: true }}
                  >
                      <motion.div 
                        className="bg-cyan-100 p-4 rounded-full mb-4"
                        whileHover={{ 
                          scale: 1.1, 
                          backgroundColor: "#67e8f9",
                          boxShadow: "0 10px 25px rgba(0,0,0,0.1)"
                        }}
                        transition={{ duration: 0.3 }}
                      >
                        {feature.icon}
                      </motion.div>
                      <motion.h3 
                        className="text-xl font-bold text-black mb-2"
                        initial={{ opacity: 0 }}
                        whileInView={{ opacity: 1 }}
                        transition={{ duration: 0.6, delay: feature.delay + 0.2 }}
                      >
                        {feature.title}
                      </motion.h3>
                      <motion.p 
                        className="text-gray-600"
                        initial={{ opacity: 0 }}
                        whileInView={{ opacity: 1 }}
                        transition={{ duration: 0.6, delay: feature.delay + 0.4 }}
                      >
                        {feature.description}
                      </motion.p>
                  </motion.div>
                ))}
            </div>
        </motion.section>

        {/* Sección 7 – Galería de Productos con animaciones */}
        <motion.section 
          className="py-20 bg-gray-100"
          initial={{ opacity: 0 }}
          whileInView={{ opacity: 1 }}
          transition={{ duration: 0.8 }}
          viewport={{ once: true }}
        >
            <div className="container mx-auto px-6">
                <motion.h2 
                  className="text-4xl font-bold text-black text-center mb-12"
                  initial={{ opacity: 0, y: 50 }}
                  whileInView={{ opacity: 1, y: 0 }}
                  transition={{ duration: 0.8 }}
                  viewport={{ once: true }}
                >
                  Galería de Soluciones
                </motion.h2>
                <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
                    {[...Array(8)].map((_, i) => (
                        <motion.div 
                          key={i} 
                          className="overflow-hidden rounded-lg shadow-md group cursor-pointer"
                          initial={{ opacity: 0, scale: 0.8 }}
                          whileInView={{ opacity: 1, scale: 1 }}
                          transition={{ duration: 0.5, delay: i * 0.1 }}
                          whileHover={{ 
                            scale: 1.05,
                            boxShadow: "0 15px 30px rgba(0,0,0,0.15)"
                          }}
                          viewport={{ once: true }}
                        >
                            <motion.img 
                              src={`https://placehold.co/600x400/E0E7FF/3730A3?text=Producto+${i+1}`} 
                              alt={`Producto ${i+1}`} 
                              className="w-full h-full object-cover transition-transform duration-500"
                              whileHover={{ scale: 1.2 }}
                            />
                        </motion.div>
                    ))}
                </div>
            </div>
        </motion.section>

        {/* Sección 8 – App Interface */}
        <section className="bg-white">
            <div className="container mx-auto px-6 py-20 grid md:grid-cols-2 gap-12 items-center">
                <div className="text-left">
                    <h2 className="text-4xl font-bold text-black mb-4">La innovación no se detiene</h2>
                    <p className="text-lg text-gray-600 mb-6">Pronto podrás llevar tu salud ortopédica en la palma de tu mano con nuestra aplicación móvil para agendar citas, seguir tu progreso y explorar nuestro catálogo.</p>
                    <ul className="space-y-2 text-gray-700">
                        <li>✓ Agenda de citas</li>
                        <li>✓ Seguimiento de pacientes</li>
                        <li>✓ Catálogo digital de productos</li>
                    </ul>
                </div>
                <div>
                    <img src="https://placehold.co/800x600/1E40AF/FFFFFF?text=Interfaz+de+App" alt="Interfaz de la aplicación" className="rounded-xl shadow-2xl"/>
                </div>
            </div>
        </section>

    </Layout>
  );
}

export default Home; 